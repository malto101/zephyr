# This board forces CONFIG_USERSPACE=y, unlike most platforms this test
# runs on, so the CONFIG_MAX_THREAD_BYTES=4 set above by this test's own
# prj.conf is too small for the kernel-object thread-permission bitmask
# (34 thread objects need at least 5 bytes).
CONFIG_MAX_THREAD_BYTES=5
