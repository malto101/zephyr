tests:
  libraries.lockfree:
    platform_exclude:
      - m2gl025_miv # renode times out
      # Times out on the Hexagon port: test_mpsc_threaded stalls after
      # 'joining mpsc thread 0'.
      - qemu_hexagon
    tags:
      - lockfree
